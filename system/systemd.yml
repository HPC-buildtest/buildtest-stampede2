version: 0.0.1
systemd_get-default:
  type: script
  description: get the name of the default target
  run: if [ "multi-user.target" == `systemctl get-default` ]; then echo "multi-user target enabled"; fi
